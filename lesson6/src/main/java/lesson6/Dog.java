package lesson6;

public class Dog extends Animal {
    Dog(String type, String name, double RunLimit, double SwimLimit, double JumpLimit) {
        super(type, name, RunLimit,SwimLimit,JumpLimit);
    }
}
